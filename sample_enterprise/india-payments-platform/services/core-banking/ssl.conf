# Core banking interface - legacy OpenSSL configuration (pending PQC hybrid rollout)
openssl_conf = openssl_init

[openssl_init]
ssl_conf = ssl_module

[ssl_module]
system_default = tls_system_default

[tls_system_default]
MinProtocol = TLSv1.2
CipherString = DEFAULT:@SECLEVEL=2
