title = "ECDAT-X secret scanning"

[extend]
useDefault = true

[allowlist]
description = "Documented placeholders in environment templates, and the shared test-only password fixture used across the auth test suite"
paths = [
  '''\.env\.example$''',
  '''\.env\.production\.example$''',
]
regexes = [
  '''Correct-Horse-Battery-2026''',
]
